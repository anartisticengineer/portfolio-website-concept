name: Run Mocha Tests
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  build:
    node-version: 18
    runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-node@v3
      run: npm install
      run: npm test